# Pylinka — Implementation Questions Log

> Append-only. When a contract is ambiguous or two docs conflict, the implementing agent records it here (date, doc §, question, proposed answer), follows document precedence (SPEC §0.1), and continues. The owner reviews and answers; answers get folded back into the docs.

| Date | Doc / § | Question | Proposed answer | Owner ruling |
|---|---|---|---|---|
| 2026-07-10 | §11.2 / §16 | `param.ref` output PortType is dynamic (a ParamDef is `f32\|vec2\|color`) but `NodeSchema` declares one fixed `outputs` type. How should the catalog express this? | v1 catalog declares `param.ref` output as `f32` (matches the §14 golden — windPower/windDir are f32). The compiler resolves the real type from the referenced `ParamDef` during SSA lowering; the validator's coercion for `param.ref` edges should consult the ParamDef type, not the schema's placeholder. Flagged for compiler (C1/C4). | — |
| 2026-07-10 | §16 math.* | Arithmetic math nodes (`add/sub/mul/div/mad/mix/clamp/min/max`) are type-polymorphic (scalar or vector) but `NodeSchema` port types are fixed and `StructuralSpec` can't add a `type` selector without "adding an option not listed". No golden exercises math.*. | v1 catalog declares binary arithmetic math nodes with **f32** ports (the common case); dedicated vector nodes (`makeVec2/4`, `component`, `splat`, `rotate2d`, `length`, `normalize`) handle vector composition/decomposition. Full vector arithmetic deferred to the compiler milestone (C4) where real goldens force a resolution (likely compile-time type inference over the connected type). | — |
| 2026-07-10 | §11.2 / §12.3 V011 | `tex.*` and `param.ref` reference project-scoped ids (assetId / ParamDef.id) that aren't enumerable at schema-definition time, but `StructuralSpec.options` is a fixed `string[]`. | Use `structural` with `options: []` (empty = "dynamic, editor populates from the project"); the validator checks the chosen value against the project's assets/params (V010/V011), not against the schema's `options`. Documented as the convention for dynamic structural references. | — |
| 2026-07-10 | §14.1 vs §11.1/§12.3 V012 | The §14 golden project (meant to be valid) defines `windPower` with `scale:"log"` **and** `min:0`, but V012 (and §11.1) require log scale to have `min > 0` — a log slider uses `value = min·(max/min)^t`, undefined at `min=0`. Contradiction inside the spec. | V012 is correct (log needs a positive min). Treating §14.1's `min:0` as a typo; the graph-package test fixture uses `windPower.min = 1` (keeps the `default:10` in range, log-valid). `min` never appears in generated WGSL (it's a runtime uniform value), so no golden-output impact. Recommend the owner patch §14.1 to `min: 1`. | — |
| 2026-07-10 | §20 Q4 (pixi view-transform hookup) | How does a custom GPU renderable inject a pass into pixi v8's render graph and get its world transform, respecting z-order? | **Resolved (architecture) in docs/SPIKE-RESULTS.md** by studying NeutrinoParticles' pixi-v8 integration (ISC): `ParticleSystemView extends ViewContainer` with `renderPipeId`/`batched=false`; a `RenderPipe` under `WebGPUPipes`/`WebGLPipes` reads `view.worldTransform` in `execute()` → `scaleOffset` uniform; `baseParent` = the world-space static layer behind `follow()`. GPU dispatch/draw bodies inside `execute()` still need a hardware run. | — |
| 2026-07-10 | §6 / §12.1 vs §14.3/§14.4 | Codegen order: §6 (topo tie-break) and §12.1 (hash) say "node.id ascending, **plain '<' string compare**". But the §14.4 golden update body emits `n7, n9, n10, n11` — with plain string compare `"n10" < "n7"`, so string order would emit `n10` first. The golden clearly uses **natural/numeric** id order. | Codegen topo tie-break uses **natural id order** (`n7 < n10`) to match the frozen §14.3/§14.4 goldens (golden > doc, §0 rule 1). The hash (`@pylinka/graph` hash.ts) keeps plain string compare per §12.1 — the two need not share a comparator: the hash⟺codegen invariant only requires codegen to be a deterministic function of hashed data (ids/kinds/edges), which natural-id order is. Init writers are emitted in canonical order (spawnPosition, initLife, initVelocity, texIndex); update writers in natural-id order. Recommend the owner reconcile §6/§12.1 wording toward "natural id order". | — |
| 2026-07-11 | §16 / §18.3 | Owner asked for vortex/helix-class forces now, but §16 marks `field.vortex` M2 and the catalog test asserted its absence. | Promoted `field.vortex` to the v1 catalog (center vec2 emitter-relative, strength/pull/radius f32, linear falloff, radius ≤ 0 = global) and added `field.turbulence` (curl of animated value noise, inlined backend-neutral arithmetic — no helper fns). §16 amended; the M2-absence test now covers `gen.curl`/`field.curlField`/`shape.drawnArea`/`math.expression` only. Note: `field.vortex` center is **emitter-relative** while `field.radial` center stays **absolute** (per original §16) — flagging the asymmetry for an owner ruling. | — |
| 2026-07-11 | §16 shape.drawnArea | Owner asked for "load an image, draw where it should emit". `shape.drawnArea` is M2 (SDF-based). | Implemented as a **system-level editor/runtime feature** (like textures/sub-emitters): editor paints a mask → weighted point table → `createParticles({ emissionMask })` texelFetch spawn. The graph catalog is untouched; `shape.drawnArea` (SDF, compiled) remains M2. | — |
| 2026-07-12 | §13.2/§13.5/§13.6 | The spec's verbatim WGSL names binding 4 `meta` — but `meta` is a **reserved WGSL keyword**; Dawn rejects the module (`'meta' is a reserved keyword`), so the scaffolds as written never ran on hardware. Found on the first real-device run of the §13 contract. | Renamed the binding to `pmeta` in the compiler scaffolds, both goldens (deliberate golden change under §0 — the frozen form was unrunnable), and the §13 spec text. No other §13 identifier collides with the WGSL reserved list. | — |
| 2026-07-12 | §11.4 webgl2 target | `CompiledSystem` has `emitSrc`/`updateSrc`, but WebGL2 has no compute stages — TF forces one fused pass. How do the fields map? | `emitSrc` = the fused transform-feedback step VERTEX shader (both generated bodies spliced: dead-slot-in-window runs INIT, alive slot runs UPDATE), `updateSrc` = the rasterizer-discard fragment stage that completes the program. Interleaved 56-byte state layout exported as `WEBGL2_LAYOUT`; the runtime advances the extra `u_spawnCursor` uniform. Generated bodies are translated from the WGSL emit (typed via collected temp types), keeping the WGSL goldens the single semantic source. | — |
| 2026-07-12 | §13.8 RenderUniforms.atlas | `.zw` are spec'd "unused"; the runtimes need a per-handle sprite size multiplier (thumbnail `sizeScale`). | `.z` carries sizeScale (default 1); `.w` stays unused. Both backends' render pipelines follow the same convention. | — |
| 2026-07-12 | §11.5 | `ParticleSystemView.recompile(compiled)` takes a CompiledSystem, but value staging needs the AUTHORED graph (UniformLayout entries point at node values); `CreateOptions.device`/`resolveTexture` need pixi-independent texture plumbing not yet designed. | Shipped `view.apply(project)` instead — hash-gates internally: value edits refresh the slot table (zero recompile), a changed graph hash swaps pipelines + resets the pool (§7.5). `createPylinka`/`createParticleSystem` require `renderer` (device derives per §7.2: WebGPU shares the GPUDevice, WebGL shares the GL context); explicit `device` + `resolveTexture` deferred. KnobBus fan-out = one shared KnobStore handed to every system's backend. `SimBackendFactory` deps carry `{system, params, knobs, renderer, device}` rather than a CompiledSystem, for the same reason. | — |
| 2026-07-12 | §7.5 / editor | A structurally-invalid INTERMEDIATE edit (e.g. two different eases mid-change: 'one ease per system' throws) killed the running effect and the editor preview with it. | `apply()` never throws on recompile failure: the previous pipelines keep running (console.warn) until the graph compiles again; the editor re-creates handles on the next edit if creation itself failed. | — |
| 2026-07-12 | pixi v8 interop (§13.13, SPIKE Q4) | How do raw GPU commands coexist with pixi's cached encoder state? | WebGPU: `encoder.finishRenderPass()` → record our own pass on pixi's command encoder (`renderTarget.adaptor.getDescriptor(rt, false, …)`, loadOp load) → `encoder.restoreRenderPass()` replays pixi's cache. Never touch the open `renderPassEncoder` mid-pass (compute submits ride the queue on a separate encoder — queue order lands them before pixi's frame submit, preserving §13.6 emit→update→draw). WebGL: raw TF step + instanced draw between pixi draws, then reset ONLY `shader/geometry/state/texture` system caches — `renderer.resetState()` also nulls the current render target mid-frame and breaks pixi's pass teardown. Verified live on both hosts (grid-under / particles / sprite-above interleaving, follow(), bursts, stats). | — |
| 2026-07-14 | §13.9 / QUESTIONS(§7.5) | The v1 compiler inlined a single `easeSel` and threw `one ease per system` when a graph mixed eases — but every `fx()` recipe wires BOTH `gen.colorOverLife` and `gen.scaleOverLife`, each with its own ease (e.g. whirlpool: color `sine.out`, scale default `linear`). So every swirl/vortex recipe failed to compile on the WebGPU/editor path, while the WebGL2 gallery runtime rendered fine (its `easeSel(int e, float t)` already dispatches per-node by index). | Lifted the limitation: the compiler now emits ONE ease function per distinct ease key used (`easeFnName`: `sine.out`→`easeSel_sine_out`), and each over-life node calls its own via `NodeCtx.ease(key)` (default `linear`). Eases are collected per-kernel (init vs update) and sorted for determinism; WebGPU emits each kernel's set, the fused WebGL2 step emits the union. Deliberate golden change (§0): the single-ease `easeSel` in the coin-spark goldens is now `easeSel_power2_out`. The old `eases.size > 1` throw is removed. | — |
