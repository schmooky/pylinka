---
'@pylinka/site': patch
---

The editor gains real undo/redo (Ctrl/Cmd+Z, Ctrl/Cmd+Shift+Z), covering deleted nodes, edges, annotations, params and node positions. Typing is left to the browser's own text undo, and rapid edits to one field collapse into a single step.
